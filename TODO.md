# ARM support project for MansOS
(these notes are for myself and will be removed when this is ready to merge)

Done:
- Able to compile Blink demo on an ARM chip running MacOS


TODO:
- Resolve the current hacks made to get the ARM target to compile
- Rewrite compile/linker logic to be more compiler-agnostic
- Clean up Makefiles and make them more platform/toolset-agnostic
- Validate that it is possible to compile to ARM in a Linux environment
- Validate that it is possible to compile to ARM in a Windows environment